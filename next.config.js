/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["example.com", "images.unsplash.com"], // Add any domains you'll use for images
  },
};

module.exports = nextConfig;
